## 1. db:去重命中刷新 unit_price

- [x] 1.1 `packages/db/src/repository.ts` 加一个**模块内私有的纯构造函数**:产出刷新 `unit_price` 的 `set` 载荷(`per100ml`/`per100g`/`formula`/`confidence`/`warnings`,`warnings` 经 `encodeJson`)与 `where unit_price.id = :id`。入参 `calc` 沿用既有 `CalcResultGate`;**禁止**触及 `product`/`product_raw` 任何列;**禁止**加任何条件谓词(见 design D2)。不进 `Repository` 公共接口——无跨包调用方
- [x] 1.2 `saveParsed` 三条命中分支各自执行刷新:sqlite 事务内 `changes=0`、D1 SELECT-first 命中、D1 抢插失败 catch 内回退命中。三处**必须**走同一构造函数(**只共用构造、禁止共用执行器**,见 design D4);**sqlite 分支的事务回调必须同步、回调体内零 `await`、刷新恰好一条语句**(一旦 UPDATE 前出现任何 `await`,该语句会逃到 COMMIT 之后且不报错);**D1 两处刷新必须 `await`**(浮动 promise 在 `/contribute` 这条非 `waitUntil` 路径上会写丢失且无错误);drizzle builder 构造后**必须**执行(忘记执行是静默 no-op)。`saveParsed` 签名与返回值形状**不变**
- [x] 1.3 刷新**抛错**时向上抛(不吞),由既有调用方语义处置(`/contribute` → `500` + `rawId`;`/ingest` 后台 → 只记日志)
- [x] 1.4 更新注释:`saveParsed`、`packages/db/src/dedupe.ts:8-9`(给 `keep oldest` 补「对 `unit_price` 仅指行与 id;`product` 行仍全列冻结」)、`packages/db/src/repository.ts:6-9` 与 `packages/db/src/schema.ts:145-147`(「never recomputed from the integer-cents price」仍成立,但需说明去重命中现在会用编排产出的 `calc` 刷新)、`apps/api/src/tagging.ts:253`(「Does NOT replay /ingest (first-write-wins…)」的理由链已被推翻,收窄为「会覆写 title/price/captured_at 并触发后台 tier2」;顺带把 `:252` 的「~376 products」改成实测 1197,并摘掉/重指 `[[ingest-write-once-needs-backfill]]` 引用)、`apps/api/src/tagging.ts:184-198`(`ADMIN_BACKFILL_MAX_LIMIT=5` 的推导把 D1 调用记进了「50 子请求」桶,前提被证伪——**只订正前提,是否上调 5 属另案**)
- [x] 1.5 **改写既有必红/失真测试**:`packages/db/src/__tests__/repository.test.ts:766-770` 的 `expect(row.per100ml).toBe(fullCalc.unitPrice.per100ml)` 反转为第二次 calc 的值 + 改 `:766` 注释(`oldest wins` 仅指行与 id);`repository.test.ts:727-728` 的 `inserts nothing` 措辞;`packages/db/src/__tests__/d1-dedupe.test.ts:188` 用例名与 **`:202`** 的 `// …returned without writing` 注释(改为「返回既有对 + 一条刷新 UPDATE;不进 batch,故 `batchCount` 仍为 0」);`packages/db/src/__tests__/taxonomy.test.ts:470` 用例名 `(first-write-wins)` → 「行与 id 收敛」。**保留**「`product.confidence` 不被刷新」那条断言并把 `:786-787` 的注释限定为 `product.confidence`。**注意**该用例的 `specLow`/`specHigh` 只差 `confidence` 而 `calculate` 不吃它,两次 calc 五列全同 → 在这里加 `unit_price.confidence` 断言是空断言;把「`product.confidence` 冻结 / `unit_price.confidence` 刷新」的分叉钉进 1.6 的新用例(那里两次价不同、band 与 formula 真会动)
- [x] 1.6 db 新增单测(**sqlite 落 `repository.test.ts`、D1 变体落 `d1-dedupe.test.ts`**,两次 calc 的五列必须可观察不同):同 `rawId` + 同 spec + **不同价** 两次 `saveParsed` → `unit_price` 仍一行、`id` 不变、五列为第二次 calc 值、`product` 与 `product_raw` 逐列不变、返回 id 对与首次相同;同价重报也照常刷新(结果等值);第二次 calc 为不可计算终态时三列写回 NULL
- [x] 1.7 db 单测:**证明同价也真的写了**——「结果等值」断言在「跳过同价写」的实现下照样绿,故必须留下可观察的写证据(sqlite 侧挂一个 `AFTER UPDATE ON unit_price` trigger 记次数,或在 D1 fake driver 上数 UPDATE 调用),断言同价重报时该计数 +1
- [x] 1.8 db 单测:**三条命中分支各注入一次刷新写抛错**,断言 `saveParsed` reject、既有 id 对与五列**保持原状**、`product`/`unit_price` 行数不变、不产生孤儿(sqlite 侧还须断言事务已回滚)
- [x] 1.9 db 单测覆盖**第三条分支**:扩 `d1-dedupe.test.ts` 既有「late bare-insert conflict rolls the whole batch back」用例,**winner 与 loser 必须用五列可观察不同的 calc**(现 fixture 两方同为 `fullCalc`,断言会在「刷新完全没实现」时照样绿),断言回退后五列 == 后到方 calc、`unitPriceId` 不变、仍一行

## 2. 文档与运维

- [x] 2.1 `docs/backfill-runbook.md`:①标题与「目的」范围从 *Taxonomy* 扩到「派生数据回填与修正」;②「两类回填(勿混)」加第三类**「单价偏差修正 = 重灌 + 检测器」**(它不是一个端点,是一个循环);③修正 `:9` 的「不重放 `/ingest`」理由链(收窄为「会覆写 title/price/captured_at 并触发后台 tier2」——first-write-wins 对 `unit_price` 已不成立);④写入检测 SQL 与循环:**直接引用 `scripts/census-drift.sql`**(唯一可跑形态;design D3 只给核心谓词,二者的谓词须保持一致)。完成判据 = **`drifted_fixable_by_reingest = 0` 且 census ④ 与「重灌 SKU − 落到 product」差集为空**(不是「空集」——`drifted_ghost` 逐轮记数、作已披露残留);写明 ④b(`formula IS NULL AND price>0`)是**人工复核清单、不是失败门**;写明「检测干净」只在暂停 ingest 的窗口内是持久结论,点名重报必须走同步 `/contribute`;⑤注明该检测是**运维侧**核对,不是系统自动校验(主 spec 的「两套金额不做跨表交叉校验」约束的是系统);⑥沿用既有「刷新 CDN」节
- [x] 2.2 把本次实测数写进 runbook:**1197 products / 1297 raws / 507 rankable**。陈旧口径**逐处**处置(**不要**一刀切):`openspec/specs/persistence/spec.md:83`「约 445 行」与 `openspec/specs/category-tagging/spec.md:189/241`(其中 `:189` 已被本变更的 category-tagging delta 逐字搬入,**改不改要显式决定**);`apps/api/src/tagging.ts:252` 见 1.4。**`docs/backfill-runbook.md:13` 的「存量 ~376 行」是对已执行完的 native-id 回填的历史记述,禁止改动**;偏差行 **69**(在榜 44);`price ≤ 0` 的 raw **26** 条(重报后会退出榜单);`formula IS NULL 且 price>0` **227 行**(人工复核清单,非失败门);幽灵行 **4 组 8 条**明细(全为重量轴/零价、`per100ml` 均 NULL、不在榜)作已披露残留
- [x] 2.3 更新 `docs/miniapp-product-form.md:56-61` 整节:`:56`「入库管道 first-write-wins、重抽不覆写已落价」把 `product_raw` 与 `unit_price` 混为一谈(前者一直是 upsert-latest);`:61`「价格刷新 upsert 为未来增项」已被本变更推翻;`:59` 的「排序降权」是既存漂移(`repository.ts` 的 orderBy 无失效项),顺手订正

## 3. 收口验证

- [x] 3.1 `pnpm -r build` + `pnpm -r test` 全绿(1.5 的必红断言已改);typecheck 无新增告警。**注**:本仓 `lint` 是 placeholder、无 eslint,D4 的两条执行禁令(事务回调零 `await`、builder 必须执行)**没有 runnable check**,靠 review 守
- [x] 3.2 **归档试跑**:把 `openspec/` 复制到临时目录跑 `openspec-cn archive update-unit-price-on-reingest -y --no-validate --json`,确认通过。`validate --strict` 抓不到「MODIFIED 块改了场景标题」与「已存在需求放在 `## 新增需求`」这两类,只有 archive 抓得到
- [x] 3.3 prod 按 design 的 Migration Plan 执行,`drifted_fixable_by_reingest` **69 → 0**。**实际走的是自重报而非 HAR 重灌**:偏差集合的 `product_raw` 侧已是新价(44/69 捕获于 2026-07-21),冻的只有派生值,故把库内既有 `(title, price, capturedAt)` 原样经同步 `/contribute` 回报即可——`product_raw` 逐字不变、不谎报新鲜度(手法已写进 runbook)。因此第 3 步 HAR 重灌与第 5 步打标签 backfill **未执行且可证明为 no-op**(`product` 行数零增长、标题未变 ⇒ 打标签输入未变)。核对:`product`/`product_raw`/`rankable` 全部零变化 ⇒ 零解析漂移;`products_without_unit_price = 0`;榜单 `per100ml` 非空 441 → 440,唯一掉行成因是 `price ≤ 0` 行刷新后转不可计算(其余零价行本就是重量轴)。已刷阿里云 CDN(根目录 Directory + `/categories` File)并预热落地榜与全部 15 个可用 category 键,`X-Cache: HIT` 且 `max-age=86400` 透传正常要点:**重灌与打标签 backfill 在入口开放时做**(重灌本身走 `/ingest/batch`),灌完**再关闸**并连跑两轮 census ②b 确认逐行相同(经验排空判据,不成立则完成判据降级为快照);部署前取基线;打标签 backfill 从空 cursor 起跑;census ②b 的 `(store, store_sku)` 明细与本轮 HAR SKU 求交;点名重报**一律走同步 `/contribute`**(**禁止** `/ingest`);终止条件 = `drifted_fixable_by_reingest = 0` **且** census ④ 与「重灌 SKU − 落到 product」差集为空;末步刷 CDN 后才开放入口。记录偏差归零前后对比、榜单行数变化及其两个成因、重灌**之后**重跑的幽灵行普查
