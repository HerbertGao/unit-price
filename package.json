{
  "name": "unit-price",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "workerd"
    ]
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "lint": "echo \"lint: placeholder (no linter configured yet)\""
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.112.0",
    "zod": "^4.4.3"
  }
}
